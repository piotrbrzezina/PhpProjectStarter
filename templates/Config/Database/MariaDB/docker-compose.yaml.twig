
    {{host}}:
        image: mariadb:{{version}}-bionic
        environment:
            MYSQL_ROOT_PASSWORD: ${MARIA_ROOT_PASSWORD:-{{password}}}
            MYSQL_USER: ${MARIA_USER:-{{user}}}
            MYSQL_PASSWORD: ${MARIA_PASSWORD:-{{password}}}
            MYSQL_DATABASE: ${MARIA_DATABASE:-{{databaseName}}}
