
    {{host}}:
        image: mongo:{{version}}
        environment:
            MONGO_INITDB_DATABASE: ${MONGO_DB:-{{databaseName}}}
            MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME:-{{user}}}
            MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-{{password}}}