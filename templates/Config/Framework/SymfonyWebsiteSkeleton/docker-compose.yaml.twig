
    nginx:
        build:
            context: .
            target: nginx
            dockerfile: ./docker/Dockerfile
        image: szula.aws.tshdev.io/{{ clientName|lower }}/{{ projectName|lower }}/nginx_app:${BUILD_TAG:-latest}
        ports:
            - "${APP_WEB_PORT:-80}:80"

    php:
        build:
            context: .
            target: builder
            dockerfile: ./docker/Dockerfile
        image: szula.aws.tshdev.io/{{ clientName|lower }}/{{ projectName|lower }}/php_app:${BUILD_TAG:-latest}