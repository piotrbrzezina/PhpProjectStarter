
    nginx:
        image: {{ dockerReadRepository }}{{ dockerRepositoryPrefix }}{{ clientName|lower }}/{{ projectName|lower }}/nginx_app:${BUILD_TAG:-latest}
        ports:
            - "${APP_WEB_PORT:-80}:80"

    php:
        image: {{ dockerReadRepository }}{{ dockerRepositoryPrefix }}{{ clientName|lower }}/{{ projectName|lower }}/php_app:${BUILD_TAG:-latest}